<h1><%=@artist.name%></h1>
<h3><%= @artist.bio %></h3>
<p><% @artist.songs.each do |song| %></p>
<%= song.name %>
<% end %>

<%= form_for @artist, method: :delete do |f| %>
<%= f.submit "Delete Artist" %>
<% end %>